{%- if section.settings.logo -%}
<a href="/" class="theme-logo">
  <img width="100%" height="100%" src="{{ section.settings.logo | img_url: '300x300' }}" alt="{{ section.settings.logo.alt | default: shop.name }}">
</a>
{%- else -%}
<a href="/">
  {{ shop.name }}
</a>
{%- endif -%}